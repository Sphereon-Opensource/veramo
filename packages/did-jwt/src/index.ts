/**
 * Provides a {@link @sphereon/did-jwt#JwtMessageHandler | plugin } for the
 * {@link @sphereon/message-handler#MessageHandler} that finds and verifies a JWT in a message.
 *
 * @packageDocumentation
 */
export { JwtMessageHandler } from './message-handler'
